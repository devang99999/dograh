name: Deploy Dograh
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, dograh]
    steps:
      - name: Restart stack
        working-directory: /home/devanggandhi2222/dograh
        run: |
          set -euo pipefail
          ./remote_up.sh
          curl -fsS https://34-14-214-236.sslip.io/api/v1/health